language: node_js
os: osx
sudo: required
services:
  - postgresql
before_script:
  - psql -c 'create database sms;' -U postgres
script:
  - npm test
  - npm run coveralls
node_js: "10"
cache:
  directories:
    - node_modules
install: npm install && npm run db:prepare
